// module.exports.counter = function(arr) {
//     return  ' there are ' + arr.length + ' arrays in arr ';
// };

// console.log(vcounter(['harsha', 'vardhan', 'reddy']));


module.exports.counter = function(arr){
    return 'there are' + arr.lenght + ' something '

};

module.exports.add = function(a,b){
 return `sume of these 2 are ${a+b}`
};

module.exports.pi= 3.142;